import { SortedResourcePipe } from './sorted-resource.pipe';

describe('SortedResourcePipe', () => {
  it('create an instance', () => {
    const pipe = new SortedResourcePipe();
    expect(pipe).toBeTruthy();
  });
});
